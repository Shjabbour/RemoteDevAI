# =============================================================================
# SENTRY CONFIGURATION
# =============================================================================
# Copy this file to your app-specific .env file and fill in the values
# Get your DSN from: https://sentry.io/settings/[org]/projects/[project]/keys/
# =============================================================================

# -----------------------------------------------------------------------------
# Required for All Applications
# -----------------------------------------------------------------------------

# Sentry DSN (Data Source Name)
# Format: https://[key]@[organization].ingest.sentry.io/[project-id]
SENTRY_DSN=

# Environment (development, staging, production)
SENTRY_ENVIRONMENT=production

# Release version (semantic versioning recommended)
SENTRY_RELEASE=1.0.0

# -----------------------------------------------------------------------------
# Source Maps Upload (Required for CI/CD)
# -----------------------------------------------------------------------------

# Sentry Auth Token (Get from: https://sentry.io/settings/account/api/auth-tokens/)
# Permissions needed: project:releases, org:read
# WARNING: Keep this secret! Do NOT commit to version control!
SENTRY_AUTH_TOKEN=

# Your Sentry organization slug
SENTRY_ORG=

# Sentry project name (specific to each app)
SENTRY_PROJECT=

# Deployment environment (for deploy tracking)
DEPLOY_ENV=production

# -----------------------------------------------------------------------------
# Application-Specific Variables
# -----------------------------------------------------------------------------

# apps/cloud (Express Backend)
# -----------------------------------------------------------------------------
# Add to: apps/cloud/.env
#
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
# SENTRY_RELEASE=1.0.0
# SENTRY_ENVIRONMENT=production
# SENTRY_IN_DEV=false  # Optional: Enable Sentry in development


# apps/web (Next.js)
# -----------------------------------------------------------------------------
# Add to: apps/web/.env.local
#
# NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
# SENTRY_RELEASE=1.0.0
# NEXT_PUBLIC_SENTRY_IN_DEV=false  # Optional: Enable Sentry in development
#
# For source maps upload (CI/CD only):
# SENTRY_AUTH_TOKEN=your_auth_token
# SENTRY_ORG=your-org
# SENTRY_PROJECT=remotedevai-web


# apps/mobile (React Native / Expo)
# -----------------------------------------------------------------------------
# Add to: apps/mobile/.env
#
# EXPO_PUBLIC_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
# EXPO_PUBLIC_SENTRY_RELEASE=1.0.0
# EXPO_PUBLIC_SENTRY_IN_DEV=false  # Optional: Enable Sentry in development


# apps/desktop (Electron)
# -----------------------------------------------------------------------------
# Add to: apps/desktop/.env
#
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
# SENTRY_RELEASE=1.0.0
# SENTRY_ENVIRONMENT=production


# -----------------------------------------------------------------------------
# Optional Configuration
# -----------------------------------------------------------------------------

# Sample Rate for Performance Monitoring (0.0 to 1.0)
# 0.1 = 10% of transactions, 1.0 = 100% of transactions
# Recommended: 1.0 for dev, 0.1 for production
SENTRY_TRACES_SAMPLE_RATE=0.1

# Sample Rate for Session Replay (0.0 to 1.0)
# Only for web apps
SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.1

# Sample Rate for Error Replay (0.0 to 1.0)
# Only for web apps
SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=1.0

# Sample Rate for Profiling (0.0 to 1.0)
# Only for backend apps
SENTRY_PROFILES_SAMPLE_RATE=0.1

# Send Default PII (Personally Identifiable Information)
# Set to false for privacy (recommended)
SENTRY_SEND_DEFAULT_PII=false

# Debug Mode (Logs Sentry operations to console)
SENTRY_DEBUG=false

# -----------------------------------------------------------------------------
# CI/CD Environment Variables (GitHub Actions, etc.)
# -----------------------------------------------------------------------------
# Add these to your CI/CD secrets:
#
# SENTRY_AUTH_TOKEN      - Auth token for source maps upload
# SENTRY_ORG             - Organization slug
# SENTRY_PROJECT         - Project name
# DEPLOY_ENV             - Deployment environment (production, staging, etc.)
# SENTRY_RELEASE         - Release version (can use git commit hash)


# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Create a Sentry account at https://sentry.io
#
# 2. Create projects for each application:
#    - remotedevai-cloud (Node.js)
#    - remotedevai-web (Next.js)
#    - remotedevai-mobile (React Native)
#    - remotedevai-desktop (Electron)
#
# 3. Get DSN for each project:
#    - Go to Settings > Projects > [Project Name] > Client Keys (DSN)
#    - Copy the DSN
#
# 4. Generate Auth Token (for source maps):
#    - Go to Settings > Auth Tokens
#    - Click "Create New Token"
#    - Name: "CI/CD Source Maps Upload"
#    - Scopes: project:releases, org:read
#    - Copy the token
#
# 5. Add environment variables:
#    - Copy relevant variables to each app's .env file
#    - Add CI/CD secrets to your repository settings
#
# 6. Test the integration:
#    - Run your app in development
#    - Trigger a test error
#    - Check Sentry dashboard for the error
#
# For detailed setup instructions, see: docs/SENTRY_SETUP.md
#
# =============================================================================
