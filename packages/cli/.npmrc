# Save exact versions in package.json
save-exact=true

# Disable package-lock.json (use pnpm or yarn lock instead if needed)
package-lock=false

# Use production builds for published package
production=false
